#!/usr/bin/osascript
# How to run: chmod +x open_mail.oas && ./open_mail.oas
on run
  tell application "Mail"
    activate
    if (count of message viewers) is 0 then
        make new message viewer at front
    end if
  end tell
end run
